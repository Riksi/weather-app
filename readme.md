To run do the following in a terminal window 

```
export RELEASE="True"
cd frontend
npm start
```

Then in another terminal window do the following from the base directory

```
streamlit run app.py
```